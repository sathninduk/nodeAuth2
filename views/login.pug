extends layout

block content
 if errors
  each error, i in errors
   div.alert.alert-danger #{error.msg}
 div.con-mid(style="width: 100%;")
  div.loginbox
    form(method='post', action='/users/login')
      div.con-mid(style="width: 100%;")
        img(src="/img/co.png" style="width: 140px; padding-top: 20px")
      .form-group
      label Username
      div.input-bottom
        input.form-control(name='username', type='text', required, placeholder='Enter Username')
      .form-group
      label Password
      input.form-control(name='password', type='password', required, placeholder='Enter Password')
      br
      div.con-mid(style="width: 100%")
        input.btn.btn-primary(type='submit', name='submit',value='Login')

  p(style="font-size: 11px; color: rgba(0,0,0,.3); margin-top: 12px;") ©nodeBase - Coduza
      br
      |*A 
      a(href="https://en.wikipedia.org/wiki/Carbon_neutrality" target="_blank" style="text-decoration: none;")
        span.carbon-link carbon neutral 
      |platform

  if user
    script.
      window.location.replace("../");